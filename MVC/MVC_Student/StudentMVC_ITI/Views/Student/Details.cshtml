@model StudentMVC_ITI.Models.ViewModels.StudentDetailsVM

@{
    ViewData["Title"] = "Student Details";
}

<div class="container mt-4">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white">
            <h4>🎓 Student Details</h4>
        </div>
        <div class="card-body">
            <p><strong>Name:</strong> @Model.StdName</p>
            <p><strong>Address:</strong> @Model.StdAddress</p>
            <p><strong>Department:</strong> @Model.DeptName</p>
            <p><strong>Manager:</strong> @Model.ManagerName</p>
            <p>
                <strong>Stage:</strong> 
                <span style="color:@Model.StageColor; font-weight: bold;">
                    @(Model.StageColor == "green" ? "Young (<25)" : "Adult (>=25)")
                </span>
            </p>
        </div>
        <div class="card-footer">
            <a class="btn btn-secondary" asp-controller="Student" asp-action="GetStsAllData">
                ⬅ Back to List
            </a>
        </div>
    </div>
</div>
